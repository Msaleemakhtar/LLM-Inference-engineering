[project]
name = "python-advance"
version = "0.1.0"
description = "Python advance project"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy",
]

[build-system]
requires = ["uv_build>=0.11.3,<0.12.0"]
build-backend = "uv_build"
